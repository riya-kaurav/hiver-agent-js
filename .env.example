# Optional. If set, reply generation (src/reply_generation.js) and the LLM
# judge (scripts/08_runLlmJudge.js) will call the real Anthropic API.
# Without it, the system still runs end to end using the extractive
# fallback reply generator - no API key is required to reproduce the
# results in reports/REPORT.md.
ANTHROPIC_API_KEY=
